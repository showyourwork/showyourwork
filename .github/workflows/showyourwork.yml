name: showyourwork

on:
  push:
    branches: [main]

jobs:

  showyourwork:
    name: Build the paper
    runs-on: ubuntu-latest
    steps:

      - name: Checkout the repo
        uses: actions/checkout@v2
        with:
          fetch-depth: 0
      
      - name: Build the paper
        uses: rodluger/showyourwork-action@main